import paths from "../paths";

export default {
    store: paths.store,
    src: paths.src,
    assets: paths.assets,
    pages: paths.pages,
    components: paths.components,
    constants: paths.constants
}
